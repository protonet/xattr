Gem::Specification.new do |s|
  s.name        = 'xattr'
  s.version     = '0.1.0'
  s.summary     = "Xattr provides the xattr (setxattr, getxattr, removexattr, listxattr) functions in a nice object-oriented wrapper."
  s.description = "Xattr provides the xattr (setxattr, getxattr, removexattr, listxattr) functions in a nice object-oriented wrapper. Ruby/DL is used so no compilation of modules is necessary."
  s.authors     = ["acrylic","Henning Thies"]
  s.email       = 'henning@protonet.info'
  s.files       = ["lib/xattr.rb"]
  s.homepage    = 'https://github.com/protonet/xattr'
end